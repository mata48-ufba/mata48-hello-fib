#include <stdio.h>
#include <stdlib.h>

int fib(int n) {

  /* code here */
    
  return -1;

}

int main(int argc, const char* argv[]) {
    int n = atoi(argv[1]);
    printf("fib(%d) = %d\n", n, fib(n));
    return 0;
}
